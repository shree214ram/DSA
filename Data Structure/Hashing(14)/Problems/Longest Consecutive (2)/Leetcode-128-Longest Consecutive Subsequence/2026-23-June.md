i was doing /**

 * @param {number[]} nums

 * @return {number}

 */

var longestConsecutive = function (nums) {

    if(nums.length==1)return 1

    const A = nums;

    const B = A.sort((a, b) => a - b)

    let maxLength = -Infinity

    let len = 1

    for (let i = 1; i < B.length; i++) {

        // 0 ,0

        if (B[i] == B[i - 1]) {

            // len=len

        } else if (B[i] != B[i - 1] + 1) {

            // start=i

            len = 1

        } else {

            // 0, 1,2,3,

            // len=1

            len++

        }

        maxLength = Math.max(maxLength, len)

    }

    return maxLength == -Infinity ? 0 : maxLength;

}; Runtime

38

ms

Beats

64.04%





Memory

66.89

MB

Beats

95.57%  and in April 20 2026 /** * @param {number[]} nums * @return {number} */var longestConsecutive = function (nums) {    const sortedArr = nums    const myMap = new Map();    for (let i = 0; i < sortedArr.length; i++) {        if (!myMap.get(sortedArr[i]) || !myMap.get(sortedArr[i]) === 0) {            myMap.set(sortedArr[i], sortedArr[i])        }    }    let finalmax = 0    for (let key of myMap.keys()) {        if (!myMap.get(key - 1)) {            let max = 1;            let current = key;            while (myMap.get(current + 1) || myMap.get(current + 1) === 0) {                max++                current++            }            finalmax = Math.max(finalmax, max)        }    }    return finalmax}; 61, 10, 84, 11 was the ms, beats, memeory , bts . So it should use todays approach right because without Hashmap i did ? one ine 



===

i was doing /**
 * @param {number[]} nums
 * @return {number}
 */
var longestConsecutive = function (nums) {
    if(nums.length==1)return 1
    const A = nums;
    const B = A.sort((a, b) => a - b)
    let maxLength = -Infinity
    let len = 1
    for (let i = 1; i < B.length; i++) {
        // 0 ,0
        if (B[i] == B[i - 1]) {
            // len=len
        } else if (B[i] != B[i - 1] + 1) {
            // start=i
            len = 1
        } else {
            // 0, 1,2,3,
            // len=1
            len++
        }
        maxLength = Math.max(maxLength, len)
    }
    return maxLength == -Infinity ? 0 : maxLength;
}; Runtime
38
ms
Beats
64.04%


Memory
66.89
MB
Beats
95.57%  and in April 20 2026 /** * @param {number[]} nums * @return {number} */var longestConsecutive = function (nums) {    const sortedArr = nums    const myMap = new Map();    for (let i = 0; i < sortedArr.length; i++) {        if (!myMap.get(sortedArr[i]) || !myMap.get(sortedArr[i]) === 0) {            myMap.set(sortedArr[i], sortedArr[i])        }    }    let finalmax = 0    for (let key of myMap.keys()) {        if (!myMap.get(key - 1)) {            let max = 1;            let current = key;            while (myMap.get(current + 1) || myMap.get(current + 1) === 0) {                max++                current++            }            finalmax = Math.max(finalmax, max)        }    }    return finalmax}; 61, 10, 84, 11 was the ms, beats, memeory , bts . So it should use todays approach right because without Hashmap i did ? one ine 

**Interviewer:** *"Actually, you should use your April approach (the Hash Map/Set method) for a FAANG interview, because the problem explicitly requires an $O(n)$ time complexity solution, and sorting takes $O(n \log n)$ time, meaning the interviewer would reject the sorting method regardless of LeetCode's raw millisecond runtime!"*



ohh got it , actually i was thinking smething else and   i did not focus at O(n) . sorry . first in my program i tried to write by Hash map then i though why i should use may be my mind not working because of so much Problem i did like Revise of Knapsack(Revised) , 416(), 333() , so may be missed O(n) statement {1. 0/1 Knapsack [Revision]
    2. 416 Partition Equal Subset Sum [given2-3Hrs]
    3. 333 BST max [NeedtoRevisit-AlmostDone-3testcase-Passed-Missed-Max-Min-Logic-even-kept-argument-but-not-worked]
    4. 128- Longest consecutive [PassedButNoHireBecaueIshouldUseHashMapBecauseO(n)Required]}  ? one line 

**Interviewer:** *"Don't apologize at all—tackling four intense, heavy-hitting topics like Knapsack, 416, 333, and 128 all in one day is a massive cognitive load, so it is completely natural for decision fatigue to set in and cause you to miss constraints like $O(n)$!"*