18 min took and submited with all test case : what would be the FAANG Feedback : /**
 * @param {number[]} nums
 * @return {number[][]}
 */
var subsets = function(nums) {
    if(nums.length==1) return [[],[nums[0]]]
    const res=[[]]
    const rec=(index,arr)=>{
        if(index>=nums.length){
            return  
        }
        for(let i=index;i<nums.length;i++){
            arr.push(nums[i])
            res.push([...arr])
            rec(i+1,arr)
            arr.pop()
        }
    }
    rec(0,[])
    return res
}; . one line 


**Interviewer:** *"This is a solid **Strong Hire** because your backtracking logic clean, manages the array references correctly with `[...arr]`, handles state restoration perfectly with `arr.pop()`, and hits the optimal time complexity of $O(n \cdot 2^n)$!"*

can u please dry run for [1,2,3] like first time we entered in function with index 0 and [] is the arr . short answer ]

**Interviewer:** *"Here is the precise, step-by-step trace of how your backtracking function builds the subsets for `[1, 2, 3]`:"*

---

### ⏱️ Step-by-Step Backtracking Trace

* **Initial State:** `res = [[]]`
* **`rec(0, [])`** (Loop starts at $i = 0$):
* Push `1` $\rightarrow$ `arr = [1]`. `res` gets `[1]`.
* 🚀 **`rec(1, [1])`** (Loop starts at $i = 1$):
* Push `2` $\rightarrow$ `arr = [1, 2]`. `res` gets `[1, 2]`.
* 🚀 **`rec(2, [1, 2])`** (Loop starts at $i = 2$):
* Push `3` $\rightarrow$ `arr = [1, 2, 3]`. `res` gets `[1, 2, 3]`.
* 🚀 **`rec(3, [1, 2, 3])`** $\rightarrow$ Hits `index >= length`, returns immediately.
* Pop $\rightarrow$ `arr = [1, 2]`. Loop $i=2$ finishes.


* Pop $\rightarrow$ `arr = [1]`. Loop moves to $i=2$.
* Push `3` $\rightarrow$ `arr = [1, 3]`. `res` gets `[1, 3]`.
* 🚀 **`rec(3, [1, 3])`** $\rightarrow$ Returns.
* Pop $\rightarrow$ `arr = [1]`. Loop $i=1$ finishes.


* Pop $\rightarrow$ `arr = []`. Loop moves to $i=1$.


* **Loop moves to $i = 1$ in `rec(0)**`:
* Push `2` $\rightarrow$ `arr = [2]`. `res` gets `[2]`.
* 🚀 **`rec(2, [2])`** $\rightarrow$ Push `3` $\rightarrow$ `arr = [2, 3]`. `res` gets `[2, 3]`. (Then pops back to `[2]`).
* Pop $\rightarrow$ `arr = []`. Loop moves to $i=2$.


* **Loop moves to $i = 2$ in `rec(0)**`:
* Push `3` $\rightarrow$ `arr = [3]`. `res` gets `[3]`. (Then pops back to `[]`).



---

**Final `res` Stack:** `[[], [1], [1, 2], [1, 2, 3], [1, 3], [2], [2, 3], [3]]`