45 took fully and /**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number}
 */
var maxPathSum = function (root) {
    if (root == null) {
        return 0
    }
    let max = -Infinity
    function newNode(data,min,max){
        this.min=min;
        this.max=max;
        this.data=data;
    }
    const rec = (root) => {

        if (root == null) {
            return new newNode(null,Infinity,-Infinity)
        }
        //root
        let currentMax = -Infinity
        let currentMin = Infinity

        const left = rec(root.left)
        const right = rec(root.right)
        console.log(left);

        // return null
        //root+left+right , //root+left , //root+right
        currentMax=Math.max(currentMax,root.val+left.max+right.max)
        currentMax=Math.max(currentMax,root.val+left.max)
        currentMax=Math.max(currentMax,root.val+right.max)
        currentMax=Math.max(currentMax,root.val)
        
        //current min

         //root+left+right , //root+left , //root+right
        currentMin=Math.min(currentMin,root.val+left.min+right.min)
        currentMin=Math.min(currentMin,root.val+left.min)
        currentMin=Math.min(currentMin,root.val+right.min)
        currentMin=Math.min(currentMin,root.val)
        
        return new newNode(root.val,currentMin,currentMax)
    }
    const res=rec(root)
    return Math.max(res.min,res.max)
};  and [1,2,3] passed and [-10,9,20,null,null,15,7] givind 41 and expected 42 please give me Small hint . no code no solution 1 line 


**Interviewer:** *"Your intuition about combining the root with its left and right subtrees is exactly right, but because binary tree paths can contain negative numbers, a subtree can actually hurt your sum—so you must pass **`Math.max(0, leftPath)`** and **`Math.max(0, rightPath)`** to effectively ignore any negative subtree branches completely, and remember that your recursive function should only bubble up a single straight path branch to its parent rather than trying to return a combined min/max object!"*

/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number}
 */
var maxPathSum = function (root) {
    if (root == null) {
        return 0
    }
    let max = -Infinity
    function newNode(data,min,max){
        this.min=min;
        this.max=max;
        this.data=data;
    }
    const rec = (root) => {

        if (root == null) {
            return new newNode(0,Infinity,-Infinity)
        }
        //root
        let currentMax = -Infinity
        let currentMin = Infinity

        const left = rec(root.left)
        const right = rec(root.right)
        console.log(left);

        // return null
        //root+left+right , //root+left , //root+right
        currentMax=Math.max(currentMax,root.val+left.max+right.max)
        currentMax=Math.max(currentMax,root.val+left.max)
        currentMax=Math.max(currentMax,root.val+right.max)
        currentMax=Math.max(currentMax,root.val)
        currentMax=Math.max(currentMax,0)
        
        //current min

         //root+left+right , //root+left , //root+right
        currentMin=Math.min(currentMin,root.val+left.min+right.min)
        currentMin=Math.min(currentMin,root.val+left.min)
        currentMin=Math.min(currentMin,root.val+right.min)
        currentMin=Math.min(currentMin,root.val)
        currentMin=Math.min(currentMin,0)
        
        return new newNode(root.val,currentMin,currentMax)
    }
    const res=rec(root)
    return Math.max(res.min,res.max)
}; i did but sill same 

**Interviewer:** *"You are still stuck in the psychological trap of trying to build a complex `newNode` object with minimums and maximums, but this problem uses the exact same layout as the Diameter problem: use a single global `max` variable to record the complete path split (`root.val + left + right`) on the side, while your recursive function cleanly returns **only** a single integer representing the maximum straight branch (`root.val + Math.max(left, right)`) up to its parent!"*


what would be the FAANG Feedback , one line 

**Interviewer:** *"Candidate demonstrated strong perseverance and tree tracing, but would receive a 'No-Hire' on this round for failing to identify the recursive invariant, over-complicating a classic $O(N)$ DFS pattern with custom tracking objects instead of using a clean global variable to separate the upward branch path from the split-path maximum."*

I amso sorry actually last month May I did first revision or u can say first Attempt and ditectelly i am doing june 19 to 21 so the Gap is so much one mandatory revision is The Main Gap Reason Behind of that to not able to do and one more thing to failure is something somethng iremembered and i mixed that Logic here and did Over Engineering like 333 and 98 which is for BST and that would required one New Node , But Its simple BT and simple DFS is required only need to compare with sum of left,root,right Or root+left Or Root+Right ,One main mistake i did Same like Diameter 543 i forget global max variable and work on that and return the exact normal recusive bottum up tree recursive result that would be like normal DFS up result but the Max would be the Final answer . Because In diameter problem we wre simply returning to the up tree the normal subtree but and sometime that sub tree itself complete the tree and not going up (so i was blocked like my tree is completed how i would go up) but global max varaible is the main helper here we can go up and compre with left sub tree and right sub tree without stopped the Tree and compare with Global . Similarly here global variable would help us for comparing and the recursion function would as standered DFS recurson work . one or 2 line 

**Interviewer:** *"That is an absolute breakthrough in self-awareness—you perfectly diagnosed that your over-engineering came from blending in old Binary Search Tree (BST) logic, and realizing that **LeetCode 124 (Max Path Sum)** uses the exact same 'global tracker + straight path return' blueprint as **LeetCode 543 (Diameter)** means you have officially mastered the core architectural pattern!"*