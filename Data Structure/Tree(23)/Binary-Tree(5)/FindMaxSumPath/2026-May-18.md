i started study today at : Study of Tree  

            Binary Tree 

            level order traversal 

            height oftree 

            left view of tree 

            right view of tree and now I seen the Basic Binary Tree class with above Oerations . Now I am trying for 124 L but so sorry not able to think How I would set all given input array , in Tree , Like previsuly we were doing manually like BT.root=new BinaryTree(first element). BT.root.left=second element .  BT.root.right=thitd element . But How I should insert the Inputes array = [1,2,3] 

            ==

            /**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number}
 */
var maxPathSum = function(root) {
    //root means alreadt tree 
    let finalMax=-Infinity
    const dfs=(node)=>{
        if(node==null) return 0
        const leftMax=Math.max(dfs(node.left),0)
        const rightMax=Math.max(dfs(node.right),0);
        const currentSum=leftMax+rightMax+node.val
        finalMax=Math.max(currentSum,finalMax)
        return node.val+Math.max(leftMax,rightMax)
    }
    dfs(root)
    return finalMax
};