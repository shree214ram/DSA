/**
 * @param {number[]} nums
 * @return {number}
 */
var rob = function(nums) {
    //base case 
    if(nums.length==1) return nums[0]
    if(nums.length==2) return Math.max(nums[0],nums[1])
    // Liner DP 
    const dp=Array(nums).fill(0)
    dp[-1]=0
    dp[0]=nums[0]
    for(let i=1;i<nums.length;i++){
        dp[i]=Math.max(dp[i-1],nums[i]+dp[i-2])
    }
    return dp[nums.length-1]
};

I think in 13 min resolved . As informed you in erlier discussin today i have started DP Revision after December and Just Seen the List of Pattern  . one line Faang Feedback