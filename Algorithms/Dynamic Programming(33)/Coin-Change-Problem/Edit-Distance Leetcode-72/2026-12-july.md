/**
 * @param {string} word1
 * @param {string} word2
 * @return {number}
 */
var minDistance = function (word1, word2) {
    /*

                h                   o       r       s       e
            0   0                   0       0       0       0 
        r   0   1(replace h with r) 1       0/1     
        o   0   0  1  2(min)
        s   0




        s1 and s2 2 string and 1d DP 
        2 for {
        
            {
                ifs1[i]==s2[j]{

                }
            }
        }
    */
    const DP = Array(s2.length).fill(0)
    DP[0] = 1
    for (let i = 1; i < s1.length; i++) {
        let pre = 0
        for (let j = 1; j < s2.length; j++) {
            let temp = DP[j]
            if (s1[i - 1] == s2[j - 1]) {
                DP[j] = pre
            } else {
                DP[j] = 1+Math.min(DP[j-1],DP[j])
            }
            pre = temp;
        }
    };
    return DP[DP.length-1]
}



====

finally I wrote from Own ButNeed to Revisit :-

/**
 * @param {string} word1
 * @param {string} word2
 * @return {number}
 */
var minDistance = function (word1, word2) {
    /*
        Interval DP with 1D and Pre value 
    */
    const s1 = word1;
    const s2 = word2;

    const DP = Array(s2 + 1).fill(0)
    //mistake1
    for (let j = 0; j <= s2.length; j++) {
        DP[j] = j
    }

    // DP[0] = 1 
    for (let i = 1; i <= s1.length; i++) {
        //pre
        let pre = DP[0];//digonal
        DP[0] = i //we need to maintain first cell for every row 
        for (let j = 1; j <= s2.length; j++) {
            //temp
            let tmp = DP[j]
            if (s1[i - 1] == s2[j - 1]) {
                DP[j] = pre
            } else {
                DP[j] = 1 + Math.min(DP[j], DP[j - 1], pre)
            }
            pre = tmp//DP[j] //mistake1
        }
    }
    // console.log(DP)
    return DP[s2.length] //
};