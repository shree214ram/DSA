<script>
//11:04 30 min remaining 
/*
Input: arr[] = [2, 1, 3, 4]
Output: 20
Explanation: There are 3 matrices of dimensions 2x1, 1x3, and 3x4, 
Let the input 3 matrices be M1, M2, and M3. There are two ways to multiply ((M1 x M2) x M3) and (M1 x (M2 x M3)), 
Please note that the result of M1 x M2 is a 2 x 3 matrix and result of (M2 x M3) is a 1 x 4 matrix.
((M1 x M2) x M3)  requires (2 x 1 x 3)  +  (2 x 3 x 4) = 30 
(M1 x (M2 x M3))  requires (1 x 3 x 4) +  (2 x 1 x 4) = 20 
The minimum of these two is 20.

*/
const A=[2,1,3,4]
const m=A.length
const DP=Array.from({length:m},()=>Array(m).fill(-1))

const rec=(i,j)=>{
	if(j-i<=1) return 0 //no trangle or 3 vertices multiplication not possibe
	//if(i>=m || j>=m) return
    
    if(DP[i][j]!==-1) return DP[i][j]
    //a,b is current vertices then c d e are the remaining from poligon 
    //i-2 j=1 , so 2*1*3 + 2*1*4
    
    /*
    	2   1
        4   3
    */
   // let currentSum=Infinity
    let min=Infinity
    for(let k=i+1;k<j ;k++){ //Q1 we need to move k middle pinter to partition array but not able to find out , till where i need to increase K 
    	const currentMul=A[i]*A[j]*A[k] + rec(i,k) + rec(k,j) //partitions
        console.log("hay")
        //currentSum=Math.min(currentMul,currentSum)
        //Q2 how to call like rec(i,k)+ rec(k+1,j)
        min=Math.min(min,currentMul)
    }
    
    return DP[i][j]=min
}
console.log(rec(0,A.length-1))

</script>